import { IChildren } from "@/interfaces/IChildren";

export default function PublicLayout({children}: IChildren){
    return(
        <>
            <>
                {children}
            </>
        </>
    )
}
