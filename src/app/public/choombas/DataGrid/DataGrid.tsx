import { IChoomba } from '@/interfaces/IChoomba'
import React from 'react'

interface Props {
    rows: IChoomba
}

const DataGrid = () => {
   return (
        <div>DataGrid</div>
   )
}
export default DataGrid
